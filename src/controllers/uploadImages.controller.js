import { asyncHandler } from "../utils/asyncHandler.js"

const uploadStaticServicesImage = asyncHandler(
    async(req, res) => {
        const {image} = req.body;

        // if()
    }
);

export default uploadStaticServicesImage